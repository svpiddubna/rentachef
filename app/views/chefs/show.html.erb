<div class="container">
  <div class="row">
    <div class="profile">
      <h3 class="h3-margin"> Hey there, I am <%= @chef.first_name %> <%= @chef.last_name %></h3>
      <div>
        <div class="col-xs-12 col-md-6">
          <div class="card-profile">
            <img src= <%= @chef.photo %> alt="Me", class="profile-pic">
            <br>
            <br>

            <div class="card-profile-text">
              <p>Location: <strong><%= @chef.location %></strong></p>
              <p>Cuisine: <strong><%= @chef.cuisine %></strong></p>
              <p>Rate: <strong><%= @chef.rate %>€/hour</strong></p>
            </div>
          </div>
        </div>
      </div>
      <div class="col-xs-12 col-md-6">
        <h3>Get to know me:</h3>
        <p><%= @chef.description %></p>

        <h3>Want me to cook for you?</h3>
        <%= simple_form_for [@chef, @booking] do |f| %>
        <%= f.input :date %>
        <%= f.hidden_field :location, :value => params[:location] %>
        <%= f.submit "Book now!", class: "btn btn-success" %>
        <% end %>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-12" style="margin: 30px 0 0 12px">
    <%= link_to "Back to search results", chefs_path(location: @location), class: "btn btn-primary" %>
  </div>
  </div>
  </div>
</div>
